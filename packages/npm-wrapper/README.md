# tgsm

`@imadtg/tgsm` is the published launcher package for `tgsm`.

Install with:

```bash
bun add -g @imadtg/tgsm
```

It resolves a matching platform package at runtime and executes the standalone `tgsm` binary for the current OS and CPU architecture.
